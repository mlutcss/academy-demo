У аватара достаточно простого состояния: оверлей скрыт через `O0` и становится видимым по `_h` у самого оверлея.

Такой вариант полезен, когда состояние относится к одному элементу и не требует именованного контекста.
