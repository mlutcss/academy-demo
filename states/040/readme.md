Добавляем первое состояние элемента: суффикс `_h` соответствует псевдоклассу `:hover`. Например, `Bgc-$secondary900_h` меняет фон кнопки закрытия только при наведении.

Хелпер `-Ts` задаёт переход, поэтому изменение фона происходит плавно. Таким же способом оформляем кнопку сохранения.
