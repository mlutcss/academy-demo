Начинаем оформлять поля формы. Селектор после имени утилиты разделяется символом `_`: `-Ts_:input,:textarea` применяет переход ко всем `input` и `textarea` внутри блока.

Это позволяет задать общее поведение полей одной группой утилит, а не повторять классы на каждом элементе.
